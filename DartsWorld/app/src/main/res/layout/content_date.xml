<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="mprog.nl.a10973710.dartsworld.DateActivity"
    tools:showIn="@layout/app_bar_date">

    <TextView
        android:paddingTop="8dp"
        android:paddingBottom="8dp"
        android:id="@+id/tvTournamentName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textStyle="bold"
        android:text="No matches today"
        android:onClick="tournamentInfoClick"
        android:textColor="@color/white"
        android:background="@android:color/black"
        android:hint="Test"/>

    <ListView
        android:id="@+id/scoreList"
        android:listSelector="@android:color/transparent"
        android:layout_width="match_parent"
        android:layout_below="@id/tvTournamentName"
        android:layout_height="match_parent"
        android:divider="@null">
    </ListView>

</RelativeLayout>
